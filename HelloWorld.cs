using System; // Using the system namespace

namespace HelloWorld { // Defining a namespace called HelloWorld
class Program {        // Defining a class called Program
  static void Main() { // Defining a method called Main
    Console.WriteLine("Hello, World!"); // Writing "Hello, World!" to the
                                        // console, Console is a class in the
                                        // System namespace
    Console.ReadKey();                  // Waiting for a key press
  }
}
}
